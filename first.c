#include <stdio.h>

int main(){
    //printf("Hello world!");



    // int a=0, b=1, c=2, d=3, e=4;

    // a = b - c + d * e;

    // printf("%d", a);

    // int a=1, b=2, c=3;

    // int answer = a + b + c;

    // printf("%d", answer);


    int numbers[3];

    numbers[0] = 10;
    numbers[1] = 20;
    numbers[2] = 30;

    for (int i = 0; i < 3; i++){
        printf("%d", numbers[i]);
    }

    return 0;
}